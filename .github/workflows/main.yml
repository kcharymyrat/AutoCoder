name: auto-coder
on: [push]
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Checkout Code

      - uses: actions/checkout@v3
      - name: Greeting Message
        run: echo "Hello, world!"
